<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Blog | Agile Ciudad Real]]></title>
  <link href="http://agile-cr.github.io/blog/categories/blog/atom.xml" rel="self"/>
  <link href="http://agile-cr.github.io/"/>
  <updated>2014-09-28T14:23:16+02:00</updated>
  <id>http://agile-cr.github.io/</id>
  <author>
    <name><![CDATA[Agile Ciudad Real]]></name>
    <email><![CDATA[agile-cr@googlegroups.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Cómo Funciona El Blog]]></title>
    <link href="http://agile-cr.github.io/blog/2014/09/28/como-funciona-el-blog/"/>
    <updated>2014-09-28T12:20:44+02:00</updated>
    <id>http://agile-cr.github.io/blog/2014/09/28/como-funciona-el-blog</id>
    <content type="html"><![CDATA[<p>En este pequeño artículo se repasará lo básico sobre cómo funciona el blog.</p>

<h2>Estructura básica</h2>

<p>Hay dos ramas:</p>

<h3>Rama Source</h3>

<p>La rama <strong>source</strong> contiene los fuentes de la página. Cualquier cambio debe realizarse o mergearse contra esta rama. Más adelante hablaremos de cómo gestionarla.</p>

<h3>Rama Master</h3>

<p>Contiene los HTML ya compilados. Normalmente la descargaremos en el directorio <code>_deploy</code> y se gestionará automáticamente.</p>

<h2>Preparación del entorno</h2>

<p>Primero nos clonaremos el repositorio y nos situaremos en la rama <code>source</code>::</p>

<pre><code>git clone git@github.com:agile-cr/agile-cr.github.io.git
cd agile-cr.github.io
git checkout -t origin/source
</code></pre>

<p>A continuación crearemos la carpeta <code>_deploy</code> con otro clon del mismo repositorio, pero en la rama <code>master</code>:</p>

<pre><code>git clone git@github.com:agile-cr/agile-cr.github.io.git _deploy
</code></pre>

<p>Finalmente, nos descargamos todo lo que necesitamos. Tendremos que tener instalado <strong>ruby</strong>, <strong>gem</strong> y <strong>bundler</strong>:</p>

<pre><code>bundle install --path vendor/bundle
</code></pre>

<h2>Realizando modificaciones</h2>

<p>En la rama <code>source</code>, podemos editar cualquier página existente. También podemos crear contenido nuevo:</p>

<h3>Posts</h3>

<p>Se crea una plantilla nueva con::</p>

<pre><code>rake new_post["Título del artículo nuevo a crear"]
</code></pre>

<p>Acordáos de añadir la propiedad <code>description</code> y <code>author</code>. Elegid las <code>categories</code> con cuidado, a ser posible coincidiendo con alguna ya existente.</p>

<p>Una vez editado, basta con comitear y pushear.</p>

<h3>Pages</h3>

<p>Se crea una plantilla nueva con::</p>

<pre><code>rake new_page[nombre_de_la_pagina]
</code></pre>

<p>Acordáos de añadir la propiedad <code>description</code> y quitar <code>date</code> y <code>comments</code>. Muy probablemente también queráis enlazarlo, modificando <code>source/_includes/custom/navigation.html</code>.</p>

<p>Una vez editado, basta con comitear y pushear.</p>

<h2>Publicar</h2>

<p>Si tenemos todo preparado, bastará con:</p>

<pre><code>rake generate
rake deploy
</code></pre>

<h2>Más información</h2>

<p>Podéis encontrar más información sobre <a href="http://octopress.org/docs/blogging/">cómo publicar en Octopress</a>, o <a href="http://daringfireball.net/projects/markdown/syntax">el formato Markdown</a>.</p>
]]></content>
  </entry>
  
</feed>
